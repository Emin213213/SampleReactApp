// import { createRoot } from "react-dom/client";
// import MainHtml from "@/mailHtml.tsx"; // Пример импорта файла внутри JS/TS

// // import Over from "./App";

// const bootElement = document.getElementById("boot") as HTMLElement;
// const boot = createRoot(bootElement);
// boot.render(<MainHtml />);
